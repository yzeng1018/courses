# 第3讲：AI产品PRD怎么写

> 线上课 | 3节 × 15分钟 | 目标学员：P6-P8产品经理

---

## 3.1 AI产品PRD和传统PRD有什么不同（~15分钟）

### 开场：一个你凭直觉就会做的决策（5分钟）

**核心叙事：**

- AI值机项目有一个简单的逻辑：AI Agent去值机，AI做不了就用传统爬虫兜底，实在不行用人工兜底。
- 这个逻辑听起来很自然，你可能凭直觉就会这么设计。
- 但问题是：如果你不把这个逻辑写进PRD，开发团队会问你一百个问题——
  - AI"做不了"的标准是什么？超时算不算？报错算不算？操作到一半卡住算不算？
  - 什么时候切到爬虫？什么时候切到人工？
  - 切换的时候用户知不知道？要不要通知？
- **核心观点：** 传统PRD写的是"产品做什么"。AI产品PRD还要写"AI做不到的时候，产品做什么"。

### 传统PRD vs AI产品PRD：三个关键差异（10分钟）

**差异一：多了"模型行为规格"**
- 传统PRD：定义功能——用户点什么，系统做什么
- AI产品PRD：还要定义模型行为——AI应该做什么，不应该做什么，遇到边界情况怎么处理
- 类比：传统PRD像写工作说明书，AI产品PRD像写行为准则

**差异二：多了"置信度设计"**
- 传统产品：功能要么成功要么失败，是确定的
- AI产品：模型的输出有一个"它自己有多确定"的维度
- 产品要决定：多确定算"够确定"？不够确定的时候怎么呈现给用户？

**差异三：多了"降级策略"**
- 传统产品：出错了显示错误页面，让用户重试
- AI产品：AI失败不等于产品失败——你可以有B方案（规则）、C方案（人工）
- 降级策略的设计质量，直接决定了用户体验的下限

---

## 3.2 模型行为规格：告诉AI什么该做、什么不该做（~15���钟）

### 为什么需要模型行为规格（5分钟）

**我的亲身故事：** 我自己做的AI旅行助手，有一次让它推荐酒店。它推荐了一家已经关门的酒店——名字、地址、描述都像模像样。

**问题出在哪？** 我没有在产品设计里定义一条规则："推荐酒店前，必须调API验证酒店是否还在营业。"

**我后来怎么修的：**
- 推荐前调酒店API做一次在线查询
- 如果API查不到 → 不是不推荐，而是加一句提示："此信息可能不够准确，建议您确认"
- 让用户自己判断，而不是假装AI一定对

**但这里有一个取舍：** 每次推荐前都调API验证，响应速度会变慢，影响用户体验。而推荐到已关门酒店毕竟是小众case。你要不要为了一个低概率事件，让所有用户都多等几秒？

**这就是AI产品的现实：tradeoff比传统产品多得多。** 传统产品的功能要么做要么不做。AI产品的每一个设计决策，都在准确性、速度、成本之间做权衡。

**这三条就是"模型行为规格"的雏形：**
1. AI做什么 → 推荐酒店前必须验证
2. AI不做什么 → 不直接推送未验证信息
3. 边界情况 → 验证失败时加提示，让用户决定
4. 取舍 → 接受延迟换准确，因为推荐错误酒店的后果比多等几秒更严重

### 模型行为规格怎么写（10分钟）

四个必写的部分：

**第一部分：AI必须做的事（Must Do）**
- AI在执行核心动作前，必须完成哪些检查？
- 值机案例：Agent必须确认航班号、日期、乘客信息匹配后才能操作
- 旅行助手案例：推荐酒店前必须调API验证

**第二部分：AI绝对不能做的事（Must Not Do）**
- 哪些行为是零容忍的？
- 值机案例：不能给错误的乘客值机；不能在用户未授权的情况下选付费座位
- 旅行助手案例：不能编造不存在的酒店价格

**第三部分：边界情况处理（Edge Cases）**
- 当AI遇到不确定的情况，默认行为是什么？
- 是继续操作还是暂停？是告诉用户还是静默失败？
- 值机案例：遇到不认识的页面 → 暂停，切换到爬虫方案

**第四部分：用户沟通规范**
- AI的输出应该用什么语气？
- 不确定的时候怎么表达？（"建议您确认" vs 直接给答案）
- 什么时候应该主动暴露AI的局限？（"我不确定这个信息是否最新"）

---

## 3.3 置信度与降级策略（~15分钟）

### 置信度：AI有多"确定"（5分钟）

**核心概念：** AI的每一个输出，背后都有一个"它自己有多确定"的数字。产品人不需要理解这个数字怎么算，但必须决定：怎么用它。

**产品决策的两个门槛：**
- **高置信度门槛**：超过这个线，AI的输出直接展示给用户
- **低置信度门槛**：低于这个线，AI的输出不展示，走降级方案
- **中间地带**：展示，但加提示——"此结果仅供参考"或"建议您确认"

**旅行助手的例子：**
- 酒店API验证成功 → 高置信度 → 直接推荐
- 酒店API查不到 → 低置信度 → 加提示"信息可能不准确，建议确认"
- 这就是一个最简单的置信度设计

### 降级策略：AI失败了，产品不能失败（5分钟）

**AI值机的三级降级，就是一个教科书级的设计：**

| 级别 | 方案 | 触发条件 | 用户感知 |
|------|------|---------|---------|
| L1 | AI Agent | 默认方案 | 自动完成，最佳体验 |
| L2 | 传统爬虫 | AI失败/超时 | 自动完成，但覆盖率有限 |
| L3 | 人工兜底 | 爬虫也失败 | 可能有延迟，但一定能完成 |

**关键设计原则：**
- 降级对用户应该尽量透明——用户不关心后面是AI还是人，他只关心值机成不成功
- 降级不是失败，是体验的优雅退化
- 最差情况的体验下限，决定了用户的信任上限

### PRD模板：把这些都写进去（5分钟）

**一份AI产品PRD的核心结构：**

| 章节 | 传统PRD | AI产品PRD多了什么 |
|------|--------|-----------------|
| 产品概述 | 一样 | — |
| 用户场景 | 一样 | — |
| 功能需求 | 功能列表 | + 模型行为规格（Must Do / Must Not / Edge Cases） |
| 交互设计 | 页面流程 | + 置信度分级展示规则 |
| 异常处理 | 错误页面 | + 降级策略（L1/L2/L3及触发条件） |
| 数据需求 | 埋点 | + 模型表现监控指标（准确率/覆盖率/降级率） |
| 发布标准 | 功能验收 | + 模型性能基准线（低于多少不上线） |

**结尾：** 这个模板不是多了很多工作量，而是把你凭直觉会想到的事情，变成团队都能执行的文档。AI值机那个"AI做不了用爬虫兜底"的逻辑，你一个人在脑子里想很简单——但如果你不写下来，10个开发会有10种理解。

---

## 配套设计

### 费曼检验
- 3.1 → 你能说出AI产品PRD和传统PRD的三个关键差��吗？
- 3.2 → 你���为你正在做的产品写出模型行为规格的四个部分吗？
- 3.3 → 你能设计一个三级降级策略吗？L1/L2/L3分别是什么？

### 短视频拆解建议
1. **"AI产品PRD和传统PRD的3个区别"** — 框架型（收藏型）
2. **"AI推荐了一家关门的酒店，我怎么修的"** — 故事型（高流量）
3. **"AI失败了，产品不能失败"** — 降级策略讲解（金句型）
4. **"告诉AI什么该做什么不该做"** — 模型行为规格速讲（干货型）
5. **"一份AI产品PRD模板，拿走就能用"** — 模板型（引流到付费课）
